# 2026-01-10 — Session log (2)

## Summary

- Replaced the homepage “Product UI Mockup” screenshot with a non-API **Text-to-Speech UI mock** based on `/podcast-mvp`.
- Fixed locale rewrite middleware to avoid breaking `/public/*` assets (e.g. images under `/mockups/*`).
- Updated `/podcast-mvp` sidebar icons for “Text to Speech” and “History”.
- Added a static fallback voice list in the real `/podcast-mvp` voice picker when not using Google/provider voices.

## Key changes (files)

- Homepage mock UI
  - `src/components/home/tts-ui-mockup.tsx` — new UI-only TTS mock; shows static “Public/Private” voice cards with generated avatar graphics (no API).
  - `src/components/home/home-page.tsx` — uses the mock UI (instead of `/public/mockups/tts-ui.png`); added section title/subtitle matching “Sample Voices” style.

- Middleware / routing
  - `src/proxy.ts` — prevents locale rewrite for public files (`.*\\..*`) so `/mockups/tts-ui.png` and similar assets can load.

- Podcast MVP UI
  - `src/components/TTSPage.tsx` — sidebar icons: “Text to Speech” → notebook icon; “History” → history icon.
  - `src/components/GoogleVoicePicker.tsx` — static voice list fallback for UI display when provider/API isn’t available.

## Verification

- `pnpm build` passes (note: one transient Next build worker `SIGSEGV` occurred; rerun succeeded).


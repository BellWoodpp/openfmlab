# 2026-01-10 — Session log (3)

## Summary

- Homepage “Text to Speech” mock is now strictly a **demo**: removed Tone/Speed/Playback/Volume, preview disclaimer, Share/Generate buttons.
- Homepage voice cards updated to match the provided 4-state design (idle → hover → loading → playing) with the overlay control centered on the avatar.
- Homepage voice list is **Public only** (removed Private tab + private voice list from the homepage mock).
- Rebranded hardcoded `voiceslab` references to `rtvox` (`rtvox.com`, “RTVox”, `support@rtvox.com`, download filename prefixes).

## Key changes (files)

- Homepage TTS mock
  - `src/components/home/tts-ui-mockup.tsx` — removed generation controls; added voice preview playback (browser `speechSynthesis`) + 4-state voice cards + centered overlay play/loading/pause.
  - `src/components/home/home-page.tsx` — increased spacing above the “Text to Speech” section.

- Branding / domain
  - `src/lib/utils.ts` — default `getBaseUrl()` fallback → `https://rtvox.com`.
  - `src/app/podcast-mvp/layout.tsx`, `src/app/voice-cloning/layout.tsx` — `metadataBase` and titles updated to RTVox.
  - `src/components/TTSPage.tsx`, `src/components/DownloadButton.tsx` + API download routes — renamed `voiceslab-*` filenames/email to `rtvox-*` / `support@rtvox.com`.

## Verification

- `pnpm build` passes.

## Notes / Next

- The homepage voice preview uses browser `speechSynthesis` (not pre-recorded audio); actual voice depends on the user’s device/OS voices.
- USPTO automated screening couldn’t be completed from this environment due to AWS WAF/CloudFront blocking; do a manual USPTO TESS/TMSearch check for “rtvox / rt-vox / rt vox” and review LIVE marks in IC 009/042.

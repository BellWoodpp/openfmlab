# 2026-01-10 — Session log (4)

## Summary

- Implemented “品牌配置集中化”: moved brand/domain/email/download-prefix into a single config (`siteConfig`) and refactored app code to reference it.
- Removed remaining hardcoded `RTVox` / `rtvox.com` / `support@rtvox.com` / `rtvox-` literals from `src/` (now one-file rebrand).

## Key changes (files)

- Central config
  - `src/lib/site-config.ts` — `brandName`, `domain`, `siteUrl`, `supportEmail`, `downloadPrefix`, `defaultDescription`.

- Refactors to `siteConfig`
  - `src/lib/utils.ts` — `getBaseUrl()` fallback uses `siteConfig.siteUrl`.
  - `src/app/podcast-mvp/layout.tsx`, `src/app/voice-cloning/layout.tsx` — `metadataBase`/titles/descriptions read from `siteConfig`.
  - `src/components/home/tts-ui-mockup.tsx` — intro copy + preview string use `siteConfig.brandName`.
  - `src/components/TTSPage.tsx`, `src/components/DownloadButton.tsx` — brand label, support email, and download filenames use `siteConfig`.
  - `src/app/api/generate/route.ts`, `src/app/api/tts/audio/[id]/route.ts`, `src/app/api/tts/shared/[shareId]/route.ts` — `Content-Disposition` filenames use `siteConfig.downloadPrefix`.

## Verification

- `pnpm exec next build` passes (note: `pnpm build` uses Turbopack and can occasionally crash with `SIGTRAP`; rerun or use the exec command for a clean check).

